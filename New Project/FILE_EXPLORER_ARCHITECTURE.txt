# 🏗️ File Explorer Architecture Diagram

```
┌─────────────────────────────────────────────────────────────────────────────┐
│                          COLLABORATIVE FILE EXPLORER                         │
│                              System Architecture                              │
└─────────────────────────────────────────────────────────────────────────────┘

┌──────────────────────────────────────────────────────────────────────────────┐
│                                  FRONTEND                                     │
└──────────────────────────────────────────────────────────────────────────────┘

    ┌─────────────────────────────────────────────────────────────┐
    │                    User Interface Layer                      │
    │                                                               │
    │  ┌─────────────────────────────────────────────────────┐   │
    │  │          FileExplorer Component (Main)               │   │
    │  │                                                       │   │
    │  │  • Recursive tree rendering                         │   │
    │  │  • Context menu management                          │   │
    │  │  • File/folder creation dialogs                     │   │
    │  │  • Inline rename input                              │   │
    │  │  • Expand/collapse animations                       │   │
    │  │  • Theme-aware styling                              │   │
    │  └─────────────────────────────────────────────────────┘   │
    │                            │                                 │
    │                            ▼                                 │
    │  ┌─────────────────────────────────────────────────────┐   │
    │  │           Supporting Components                      │   │
    │  ├─────────────────────────────────────────────────────┤   │
    │  │  • ContextMenu     → Right-click actions           │   │
    │  │  • NewNodeDialog   → Create file/folder modal      │   │
    │  │  • TreeNode        → Individual file/folder item   │   │
    │  └─────────────────────────────────────────────────────┘   │
    └───────────────────────────────────────────────────────────────┘
                                    │
                                    ▼
    ┌─────────────────────────────────────────────────────────────┐
    │                    State Management Layer                     │
    │                                                               │
    │  ┌─────────────────────────────────────────────────────┐   │
    │  │      useFileExplorer Hook (Custom Hook)             │   │
    │  │                                                       │   │
    │  │  State:                                              │   │
    │  │  • fileStructure      → Tree data                   │   │
    │  │  • expandedFolders    → Set of open folders         │   │
    │  │  • selectedNode       → Currently selected item     │   │
    │  │  • renamingNode       → Item being renamed          │   │
    │  │  • contextMenu        → Right-click menu state      │   │
    │  │                                                       │   │
    │  │  Methods:                                            │   │
    │  │  • createNode()       → Create file/folder          │   │
    │  │  • renameNode()       → Rename item                 │   │
    │  │  • deleteNode()       → Delete item                 │   │
    │  │  • toggleFolder()     → Expand/collapse             │   │
    │  │  • selectNode()       → Select item                 │   │
    │  └─────────────────────────────────────────────────────┘   │
    └───────────────────────────────────────────────────────────────┘
                                    │
                                    ▼
    ┌─────────────────────────────────────────────────────────────┐
    │                     Service Layer                            │
    │                                                               │
    │  ┌─────────────────────────────────────────────────────┐   │
    │  │              fileAPI Service                         │   │
    │  │                                                       │   │
    │  │  • getFileStructure(projectId)                      │   │
    │  │  • createFileOrFolder(projectId, data)              │   │
    │  │  • renameFileOrFolder(projectId, data)              │   │
    │  │  • deleteFileOrFolder(projectId, data)              │   │
    │  │  • getFileContent(projectId, path)                  │   │
    │  │  • updateFileContent(projectId, data)               │   │
    │  └─────────────────────────────────────────────────────┘   │
    └───────────────────────────────────────────────────────────────┘
                                    │
                                    ▼
    ┌─────────────────────────────────────────────────────────────┐
    │                     Utility Layer                            │
    │                                                               │
    │  ┌─────────────────────────────────────────────────────┐   │
    │  │         fileExplorerIcons Utility                    │   │
    │  │                                                       │   │
    │  │  • getFileIcon(filename)       → Icon for file type │   │
    │  │  • getFolderIcon(theme)        → Themed folder icon │   │
    │  │  • getIconStyle(type, theme)   → CSS for icon       │   │
    │  │  • getFileExtension(filename)  → Extract extension  │   │
    │  │  • isCodeFile(filename)        → Check if code      │   │
    │  └─────────────────────────────────────────────────────┘   │
    └───────────────────────────────────────────────────────────────┘

            │                                            │
            │                                            │
         HTTP/HTTPS                                 WebSocket
            │                                            │
            ▼                                            ▼

┌──────────────────────────────────────────────────────────────────────────────┐
│                                  BACKEND                                      │
└──────────────────────────────────────────────────────────────────────────────┘

    ┌─────────────────────────────────────────────────────────────┐
    │                     API Layer (Express)                      │
    │                                                               │
    │  ┌─────────────────────────────────────────────────────┐   │
    │  │         Filesystem Routes (/api/filesystem)         │   │
    │  │                                                       │   │
    │  │  GET    /:projectId           → Get tree            │   │
    │  │  POST   /:projectId/create    → Create item         │   │
    │  │  PUT    /:projectId/rename    → Rename item         │   │
    │  │  DELETE /:projectId/delete    → Delete item         │   │
    │  │  GET    /:projectId/file      → Get content         │   │
    │  │  PUT    /:projectId/file      → Update content      │   │
    │  └─────────────────────────────────────────────────────┘   │
    └───────────────────────────────────────────────────────────────┘
                                    │
                                    ▼
    ┌─────────────────────────────────────────────────────────────┐
    │                    Controller Layer                          │
    │                                                               │
    │  ┌─────────────────────────────────────────────────────┐   │
    │  │        FileSystemController                          │   │
    │  │                                                       │   │
    │  │  • getFileStructure()      → Load tree from disk    │   │
    │  │  • createFileOrFolder()    → Add to tree            │   │
    │  │  • renameFileOrFolder()    → Update name in tree    │   │
    │  │  • deleteFileOrFolder()    → Remove from tree       │   │
    │  │  • getFileContent()        → Read file data         │   │
    │  │  • updateFileContent()     → Write file data        │   │
    │  │                                                       │   │
    │  │  Helpers:                                            │   │
    │  │  • findNodeByPath()        → Navigate tree          │   │
    │  │  • findParentNode()        → Get parent             │   │
    │  │  • loadStructure()         → Read from disk         │   │
    │  │  • saveStructure()         → Write to disk          │   │
    │  └─────────────────────────────────────────────────────┘   │
    └───────────────────────────────────────────────────────────────┘
                                    │
                                    ▼
    ┌─────────────────────────────────────────────────────────────┐
    │                    Storage Layer                             │
    │                                                               │
    │  ┌─────────────────────────────────────────────────────┐   │
    │  │         File System (JSON Storage)                   │   │
    │  │                                                       │   │
    │  │  Location: /backend/uploads/file-structures/        │   │
    │  │                                                       │   │
    │  │  Structure:                                          │   │
    │  │  {                                                   │   │
    │  │    "id": "root",                                     │   │
    │  │    "name": "root",                                   │   │
    │  │    "type": "folder",                                 │   │
    │  │    "children": [                                     │   │
    │  │      {                                               │   │
    │  │        "id": "unique_id",                            │   │
    │  │        "name": "file.js",                            │   │
    │  │        "type": "file",                               │   │
    │  │        "content": "..."                              │   │
    │  │      }                                               │   │
    │  │    ]                                                 │   │
    │  │  }                                                   │   │
    │  └─────────────────────────────────────────────────────┘   │
    └───────────────────────────────────────────────────────────────┘


    ┌─────────────────────────────────────────────────────────────┐
    │            Real-Time Layer (Socket.IO)                       │
    │                                                               │
    │  ┌─────────────────────────────────────────────────────┐   │
    │  │          SocketHandlers Service                      │   │
    │  │                                                       │   │
    │  │  Connection Events:                                  │   │
    │  │  • join-project          → Join project room        │   │
    │  │  • leave-project         → Leave project room       │   │
    │  │                                                       │   │
    │  │  File System Events:                                 │   │
    │  │  • filesystem:created    → File/folder created      │   │
    │  │  • filesystem:renamed    → File/folder renamed      │   │
    │  │  • filesystem:deleted    → File/folder deleted      │   │
    │  │  • filesystem:file-updated → File content changed   │   │
    │  │                                                       │   │
    │  │  Rooms: project:{projectId}                          │   │
    │  │  Broadcast: To all users in same project            │   │
    │  └─────────────────────────────────────────────────────┘   │
    └───────────────────────────────────────────────────────────────┘


┌──────────────────────────────────────────────────────────────────────────────┐
│                            DATA FLOW DIAGRAM                                  │
└──────────────────────────────────────────────────────────────────────────────┘

USER ACTION (Create File):

    [User clicks "New File" button]
              │
              ▼
    [FileExplorer component]
              │
              ▼
    [useFileExplorer.createNode()]
              │
              ├──────────────────────────────────────────┐
              │                                           │
              ▼                                           ▼
    [fileAPI.createFileOrFolder()]            [Optimistic UI update]
              │                                           │
              ▼                                           │
    POST /api/filesystem/:projectId/create               │
              │                                           │
              ▼                                           │
    [FileSystemController.createFileOrFolder()]          │
              │                                           │
              ├───► [Add to tree structure]               │
              │                                           │
              ├───► [Save to JSON file]                   │
              │                                           │
              ├───► [Return success response]             │
              │                                           │
              └───► [Emit socket event]                   │
                            │                             │
                            ▼                             ▼
                    [io.to(projectId).emit(              │
                     'filesystem:created')]              │
                            │                             │
                            ▼                             ▼
              ┌──────────────────────────────────────────┘
              │
              ▼
    [All connected clients receive event]
              │
              ▼
    [useFileExplorer socket listener]
              │
              ▼
    [Update fileStructure state]
              │
              ▼
    [Re-render FileExplorer]
              │
              ▼
    [New file appears in all user's explorers!] ✨


REAL-TIME SYNC FLOW:

    User A                          Server                      User B
      │                               │                           │
      │──── Create "test.js" ────────▶│                           │
      │                               │                           │
      │◀──── Success Response ────────│                           │
      │                               │                           │
      │         [UI updates]          │                           │
      │                               │                           │
      │                               │──── Socket Broadcast ────▶│
      │                               │    'filesystem:created'   │
      │                               │                           │
      │                               │                           │◀─┐
      │                               │                           │  │
      │                               │                           │  │ Listener
      │                               │                           │  │ triggered
      │                               │                           │  │
      │                               │                           │──┘
      │                               │                           │
      │                               │                  [UI updates automatically]
      │                               │                           │
      │                               │         "test.js appears!" ✨
      │                               │                           │


┌──────────────────────────────────────────────────────────────────────────────┐
│                        COMPONENT HIERARCHY                                    │
└──────────────────────────────────────────────────────────────────────────────┘

Dashboard/Page
    │
    └── FileExplorer
            │
            ├── Header
            │     ├── Title
            │     └── Action Buttons (New File, New Folder)
            │
            ├── File Tree
            │     │
            │     └── TreeNode (Recursive)
            │           ├── Expand Icon (chevron)
            │           ├── File/Folder Icon
            │           ├── Node Name / Rename Input
            │           │
            │           └── Children (if folder & expanded)
            │                 └── TreeNode (Recursive)
            │                       └── ... (infinite nesting)
            │
            ├── Context Menu (conditional)
            │     ├── New File (if folder)
            │     ├── New Folder (if folder)
            │     ├── Rename
            │     └── Delete
            │
            └── New Node Dialog (conditional)
                  ├── Title
                  ├── Input Field
                  └── Action Buttons (Cancel, Create)


┌──────────────────────────────────────────────────────────────────────────────┐
│                         TECHNOLOGY STACK                                      │
└──────────────────────────────────────────────────────────────────────────────┘

Frontend:
    • React 18.2.0              → UI framework
    • Framer Motion 10.18.0     → Smooth animations
    • React Icons 5.5.0         → Icons (chevrons, etc.)
    • Socket.io Client 4.7.2    → Real-time communication
    • Axios 1.6.2               → HTTP requests
    • React Hot Toast 2.4.1     → Notifications

Backend:
    • Express 4.18.2            → Web framework
    • Socket.io 4.7.2           → WebSocket server
    • Node.js (ES Modules)      → Runtime
    • File System (fs/promises) → JSON storage

Styling:
    • CSS3 (Custom)             → Professional styling
    • CSS Variables             → Theme support
    • CSS Animations            → Smooth transitions
    • Flexbox                   → Layout


┌──────────────────────────────────────────────────────────────────────────────┐
│                       SECURITY & PERFORMANCE                                  │
└──────────────────────────────────────────────────────────────────────────────┘

Security:
    ✅ CORS configured
    ✅ Token-based authentication ready
    ⚠️  Add auth middleware in production
    ⚠️  Add rate limiting per user
    ⚠️  Sanitize file paths
    ⚠️  Validate file names
    ⚠️  Implement permissions

Performance:
    ✅ Lazy rendering (folders only render when expanded)
    ✅ Optimistic updates (UI before server response)
    ✅ Efficient tree manipulation
    ✅ Memoized callbacks
    ✅ Hardware-accelerated CSS animations
    ✅ Virtual scrolling ready
    ✅ Debounced socket events


┌──────────────────────────────────────────────────────────────────────────────┐
│                           SCALABILITY                                         │
└──────────────────────────────────────────────────────────────────────────────┘

Current (MVP):
    • JSON file storage
    • In-memory tree structure
    • File-based persistence
    • Supports 100s of files per project

Future Enhancements:
    • MongoDB/PostgreSQL storage
    • Redis caching
    • CDN for icons
    • Lazy loading folder contents
    • Virtual scrolling for large trees
    • Background sync workers
    • File chunking for large files


┌──────────────────────────────────────────────────────────────────────────────┐
│                         DEPLOYMENT ARCHITECTURE                               │
└──────────────────────────────────────────────────────────────────────────────┘

Development:
    localhost:5173 (Frontend) ──────┐
                                     │
                                     ├──→ localhost:5000 (Backend)
                                     │         ↓
    localhost:5173 (Frontend) ──────┘    [File System]
                                          [Socket.IO]

Production:
    app.yoursite.com (CDN) ──────┐
                                  │
                                  ├──→ api.yoursite.com (Load Balancer)
                                  │         ↓
    app.yoursite.com (CDN) ──────┘    [Backend Cluster]
                                            ↓
                                      [MongoDB/Redis]
                                      [Socket.IO Cluster]


━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
End of Architecture Diagram
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
